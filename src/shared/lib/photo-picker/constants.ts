/** Extensions for input[type="file"] accept attribute */
export const ACCEPT_IMAGE = '.jpeg,.jpg,.png';

/** MIME types for client-side validation */
export const ACCEPTED_TYPES = ['image/jpeg', 'image/jpg', 'image/png'];

/** Max size of one file in bytes (20 MB) */
export const MAX_FILE_SIZE_BYTES = 20 * 1024 * 1024;

/** Max photos in multiple mode */
export const MAX_PHOTOS_MULTIPLE = 10;
