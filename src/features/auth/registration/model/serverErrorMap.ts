export const serverErrorMap: Record<string, string> = {
  'User with this username is already registered':
    'ServerErrors.usernameExists',
  'User with this email is already registered': 'ServerErrors.emailExists',
};
